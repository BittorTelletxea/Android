<!-- res/drawable/button_translucent_grey.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo gris translúcido -->
    <solid android:color="#00000000" /> <!-- 50% opacidad -->

    <!-- Esquinas redondeadas -->
    <corners android:radius="24dp" />

    <!-- Borde blanco -->
    <stroke
        android:width="2dp"
        android:color="#FFFFFFFF" />

    <!-- Espaciado interno -->
    <padding
        android:left="16dp"
        android:right="16dp"
        android:top="8dp"
        android:bottom="8dp" />
</shape>

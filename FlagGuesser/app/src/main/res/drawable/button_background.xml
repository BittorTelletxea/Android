<!-- res/drawable/button_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color gris translúcido -->
    <solid android:color="#80B0B0B0" /> <!-- #80 = 50% de transparencia -->

    <!-- Bordes redondeados -->
    <corners android:radius="20dp" />

    <!-- Sombra suave (solo visible en Android 9+) -->
    <padding
        android:left="16dp"
        android:right="16dp"
        android:top="8dp"
        android:bottom="8dp" />
</shape>
